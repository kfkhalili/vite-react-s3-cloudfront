schema {
    query: Query
    mutation: Mutation
}
  
type Query {
    getUser(id: ID!): User
}
  
type Mutation {
    registerUser(email: String!, password: String!): UserResponse!
    authUser(email: String!, password: String!): AuthResponse!
}
  
type UserResponse {
    success: Boolean!
    message: String!
    user: User
}
  
type AuthResponse {
    success: Boolean!
    message: String!
    token: String
}
  
type User {
    id: ID!
    email: String!
}
  